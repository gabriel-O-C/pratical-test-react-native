import styled from 'styled-components/native';

export const ProfileImage = styled.Image`
  width: ${props => props.theme.sizes.xlarge};
  height: ${props => props.theme.sizes.xlarge};
  margin-right: ${props => props.theme.sizes.small};
`;
